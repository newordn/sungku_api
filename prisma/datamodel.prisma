type User {
   id: ID! @id
   name:String
   phone: String! @unique
   password: String
   code: String
   account : Account @relation(link: TABLE)
}
type Account {
   id: ID! @id
   balance: Float!
   owner : User!
   createdAt: DateTime! @createdAt
}